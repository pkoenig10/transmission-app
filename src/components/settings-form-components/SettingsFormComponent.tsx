import {FormComponentProps} from "../form-components/FormComponent";
import {TransmissionSettingsProps} from "../TransmissionSettings";

export interface SettingsFormComponentProps extends FormComponentProps, TransmissionSettingsProps {}
